-- Referenz zur angewendeten Remote-Migration:
-- Tabelle `contacts` + `notes`, Migration der Daten aus `contact_requests` / `customers`,
-- Später: phone -> telefonnummer, privacy -> datenschutz
-- Orphan property_values bereinigt.

SELECT 1;
